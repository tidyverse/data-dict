% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{dd_validate_data}
\alias{dd_validate_data}
\title{Validate a dataset's values against its data dictionary}
\usage{
dd_validate_data(
  dict = ".",
  table = NULL,
  html = tempfile(fileext = ".html"),
  browse = interactive()
)
}
\arguments{
\item{dict}{A \code{data-dict.yaml} file, or a directory holding one.}

\item{table}{Validate only this table, instead of every table in the
dictionary.}

\item{html}{Where to write the report.}

\item{browse}{Whether to open the report with \code{\link[utils:browseURL]{utils::browseURL()}}.}
}
\value{
A list with the \code{html} path and the CLI's exit \code{status}, where \code{0}
means the dataset validated, invisibly.
}
\description{
Runs \verb{data-dict validate-data}, writes the findings as a self-contained HTML
report, and opens it in a browser. The data files themselves come from each
table's \verb{source: parquet:} entry in the dictionary, resolved relative to the
dictionary's directory.
}
\details{
A dataset that fails validation is not an R error: the report is the point,
so check \code{status} to see whether the run passed.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# a directory holding a data-dict.yaml and the parquet files it points at
dd_validate_data("inst/data")
\dontshow{\}) # examplesIf}
}
