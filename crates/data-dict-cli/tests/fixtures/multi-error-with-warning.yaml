# expected: two errors (S07, S08) and a warning (S09), in emission order.
$version: 0.1.0
tables:
  measurements:
    source:
      parquet: measurements.parquet
    columns:
      - name: id
        type: number(id)
      - name: name
        type: string
        examples: [a, b]
        units: kg
