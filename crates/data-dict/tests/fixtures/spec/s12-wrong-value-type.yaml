# expected: fails S12 - a `number` column's `examples` contains a string.

$version: 0.1.0
$learn_more: http://data-dict.tidyverse.org/

tables:
  t:
    description: A table.
    source:
      parquet: t.parquet
    columns:
      - name: count
        type: number
        examples: [1, two, 3]
