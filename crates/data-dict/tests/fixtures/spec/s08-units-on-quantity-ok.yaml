# expected: validates cleanly. `units` is allowed on a `number(quantity)` column.

$version: "0.1.0"

tables:
  measurements:
    source:
      parquet: measurements.parquet
    columns:
      - name: mass
        type: number(quantity)
        units: g
        range: [0, 5000]
