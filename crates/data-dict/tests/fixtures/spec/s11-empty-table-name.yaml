# expected: fails S11 - the table name is empty.

$version: 0.1.0
$learn_more: http://data-dict.tidyverse.org/

tables:
  "":
    description: A table with no name.
    source:
      parquet: t.parquet
    columns:
      - name: id
        type: number(id)
        examples: [1, 2, 3]
