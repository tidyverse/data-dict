# expected: fails S07 - `string` and `number(id)` columns must have an `examples` property.

$version: 0.1.0

tables:
  table:
    columns:
      - name: label
        type: string
      - name: code
        type: number(id)
