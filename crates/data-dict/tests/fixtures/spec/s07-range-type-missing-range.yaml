# expected: fails S07 - `number(quantity)` and `date` columns must have a `range` property.

$version: 0.1.0

tables:
  table:
    source:
      parquet: t.parquet
    columns:
      - name: weight
        type: number(quantity)
      - name: recorded_at
        type: date
