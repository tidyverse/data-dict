# expected: fails S11 - a column has an empty `name`.

$version: 0.1.0
$learn_more: http://data-dict.tidyverse.org/

tables:
  t:
    description: A table.
    source:
      parquet: t.parquet
    columns:
      - name: ""
        type: string
        examples: [a, b, c]
