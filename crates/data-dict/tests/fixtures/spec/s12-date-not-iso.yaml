# expected: fails S12 - a `date` column's `range` bound is not an ISO 8601 date.

$version: 0.1.0
$learn_more: http://data-dict.tidyverse.org/

tables:
  table:
    source:
      parquet: t.parquet
    columns:
      - name: seen_on
        type: date
        range: ["2020-01-01", "20-01-2021"]
