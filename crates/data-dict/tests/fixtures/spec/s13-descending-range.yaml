# expected: fails S13 - the range minimum is greater than its maximum.

$version: 0.1.0
$learn_more: http://data-dict.tidyverse.org/

tables:
  table:
    source:
      parquet: t.parquet
    columns:
      - name: mass
        type: number(quantity)
        units: kg
        range: [100, 10]
