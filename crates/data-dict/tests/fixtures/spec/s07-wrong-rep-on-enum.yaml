# expected: fails S07 - an `enum` column must use `values`, not `range` or `examples`.

$version: 0.1.0

tables:
  t:
    description: A table.
    source:
      parquet: t.parquet
    columns:
      - name: status
        type: enum
        description: A status field.
        range: [0, 10]
