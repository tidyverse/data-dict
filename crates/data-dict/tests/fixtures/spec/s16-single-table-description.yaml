# expected: validates with warnings - a single-table dictionary should carry
# its description and details at the top level, not on the table (S16).

$version: 0.1.0
$learn_more: http://data-dict.tidyverse.org/

tables:
  food:
    description: Each row is a food item.
    details: Collected from the USDA FoodData Central database.
    columns:
      - name: id
        type: number(id)
        examples: [1, 2, 3]
